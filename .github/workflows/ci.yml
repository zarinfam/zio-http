# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['**']
    types: [opened, synchronize, reopened, edited]
  push:
    branches: ['**']
    tags: [v*]

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  run_jmh_benchmark_Current_CookieDecodeBenchmark:
    name: Jmh Benchmark Current CookieDecodeBenchmark
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f CookieDecodeBenchmark.txt
          cat > CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 CookieDecodeBenchmark" | grep "thrpt" >> ../CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCollectEval" | grep "thrpt" >> ../CookieDecodeBenchmark.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_CookieDecodeBenchmark
          path: CookieDecodeBenchmark.txt

  run_jmh_benchmark_Current_HttpCombineEval:
    name: Jmh Benchmark Current HttpCombineEval
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f HttpCombineEval.txt
          cat > HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCombineEval" | grep "thrpt" >> ../HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpNestedFlatMapEval" | grep "thrpt" >> ../HttpCombineEval.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_HttpCombineEval
          path: HttpCombineEval.txt

  run_jmh_benchmark_Current_HttpRouteTextPerf:
    name: Jmh Benchmark Current HttpRouteTextPerf
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f HttpRouteTextPerf.txt
          cat > HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpRouteTextPerf" | grep "thrpt" >> ../HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 ProbeContentTypeBenchmark" | grep "thrpt" >> ../HttpRouteTextPerf.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_HttpRouteTextPerf
          path: HttpRouteTextPerf.txt

  jmh_publish_Current:
    name: Jmh Publish Current
    needs: [run_jmh_benchmark_Current_CookieDecodeBenchmark, run_jmh_benchmark_Current_HttpCombineEval, run_jmh_benchmark_Current_HttpRouteTextPerf]
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_CookieDecodeBenchmark

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_HttpCombineEval

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_HttpRouteTextPerf

      - name: Create Comment
        id: create_comment
        run: |
          rm -f body.txt
          cat > body.txt
          echo "::set-output name=res::$(echo "$(<body.txt)")"

      
      - name: Result CookieDecodeBenchmark
        id: result_CookieDecodeBenchmark
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body.txt
           done < CookieDecodeBenchmark.txt

      - name: Result HttpCombineEval
        id: result_HttpCombineEval
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body.txt
           done < HttpCombineEval.txt

      - name: Result HttpRouteTextPerf
        id: result_HttpRouteTextPerf
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body.txt
           done < HttpRouteTextPerf.txt

      - name: Set Output
        id: set_output
        run: |
          body=$(cat body.txt)
           body="${body//'%'/'%25'}"
           body="${body//$'\n'/'%0A'}"
           body="${body//$'\r'/'%0D'}"
           echo "$body"
           echo "::set-output name=body::$(echo "$body")"

      - uses: peter-evans/commit-comment@v1
        with:
          sha: ${{github.sha}}
          body: |

            **🚀 Jmh Benchmark:**

            - **Current Branch**:
             ${{steps.set_output.outputs.body}}

  run_jmh_benchmark_Main_CookieDecodeBenchmark:
    name: Jmh Benchmark Main CookieDecodeBenchmark
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f CookieDecodeBenchmark.txt
          cat > CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 CookieDecodeBenchmark" | grep "thrpt" >> ../CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCollectEval" | grep "thrpt" >> ../CookieDecodeBenchmark.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_CookieDecodeBenchmark
          path: CookieDecodeBenchmark.txt

  run_jmh_benchmark_Main_HttpCombineEval:
    name: Jmh Benchmark Main HttpCombineEval
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f HttpCombineEval.txt
          cat > HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCombineEval" | grep "thrpt" >> ../HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpNestedFlatMapEval" | grep "thrpt" >> ../HttpCombineEval.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_HttpCombineEval
          path: HttpCombineEval.txt

  run_jmh_benchmark_Main_HttpRouteTextPerf:
    name: Jmh Benchmark Main HttpRouteTextPerf
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f HttpRouteTextPerf.txt
          cat > HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpRouteTextPerf" | grep "thrpt" >> ../HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 ProbeContentTypeBenchmark" | grep "thrpt" >> ../HttpRouteTextPerf.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_HttpRouteTextPerf
          path: HttpRouteTextPerf.txt

  jmh_publish_Main:
    name: Jmh Publish Main
    needs: [run_jmh_benchmark_Main_CookieDecodeBenchmark, run_jmh_benchmark_Main_HttpCombineEval, run_jmh_benchmark_Main_HttpRouteTextPerf]
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_CookieDecodeBenchmark

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_HttpCombineEval

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_HttpRouteTextPerf

      - name: Create Comment
        id: create_comment
        run: |
          rm -f body.txt
          cat > body.txt
          echo "::set-output name=res::$(echo "$(<body.txt)")"

      
      - name: Result CookieDecodeBenchmark
        id: result_CookieDecodeBenchmark
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body.txt
           done < CookieDecodeBenchmark.txt

      - name: Result HttpCombineEval
        id: result_HttpCombineEval
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body.txt
           done < HttpCombineEval.txt

      - name: Result HttpRouteTextPerf
        id: result_HttpRouteTextPerf
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body.txt
           done < HttpRouteTextPerf.txt

      - name: Set Output
        id: set_output
        run: |
          body=$(cat body.txt)
           body="${body//'%'/'%25'}"
           body="${body//$'\r'/'%0D'}"
           echo "$body"
           echo "::set-output name=body::$(echo "$body")"

      - uses: peter-evans/commit-comment@v1
        with:
          sha: ${{github.sha}}
          body: |

            **🚀 Jmh Benchmark:**

            - **Current Branch**:
             ${{steps.set_output.outputs.body}}
