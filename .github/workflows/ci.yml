# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['**']
    types: [opened, synchronize, reopened, edited]
  push:
    branches: ['**']
    tags: [v*]

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  run_jmh_benchmark_Current_CookieDecodeBenchmark:
    name: Jmh Benchmark Current CookieDecodeBenchmark
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f Current_CookieDecodeBenchmark.txt
          cat > Current_CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 CookieDecodeBenchmark" | grep "thrpt" >> ../Current_CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCollectEval" | grep "thrpt" >> ../Current_CookieDecodeBenchmark.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_Current_CookieDecodeBenchmark
          path: Current_CookieDecodeBenchmark.txt

  run_jmh_benchmark_Current_HttpCombineEval:
    name: Jmh Benchmark Current HttpCombineEval
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f Current_HttpCombineEval.txt
          cat > Current_HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCombineEval" | grep "thrpt" >> ../Current_HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpNestedFlatMapEval" | grep "thrpt" >> ../Current_HttpCombineEval.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_Current_HttpCombineEval
          path: Current_HttpCombineEval.txt

  run_jmh_benchmark_Current_HttpRouteTextPerf:
    name: Jmh Benchmark Current HttpRouteTextPerf
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f Current_HttpRouteTextPerf.txt
          cat > Current_HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpRouteTextPerf" | grep "thrpt" >> ../Current_HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 ProbeContentTypeBenchmark" | grep "thrpt" >> ../Current_HttpRouteTextPerf.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_Current_HttpRouteTextPerf
          path: Current_HttpRouteTextPerf.txt

  run_jmh_benchmark_Main_CookieDecodeBenchmark:
    name: Jmh Benchmark Main CookieDecodeBenchmark
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@main
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f Main_CookieDecodeBenchmark.txt
          cat > Main_CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 CookieDecodeBenchmark" | grep "thrpt" >> ../Main_CookieDecodeBenchmark.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCollectEval" | grep "thrpt" >> ../Main_CookieDecodeBenchmark.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_Main_CookieDecodeBenchmark
          path: Main_CookieDecodeBenchmark.txt

  run_jmh_benchmark_Main_HttpCombineEval:
    name: Jmh Benchmark Main HttpCombineEval
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@main
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f Main_HttpCombineEval.txt
          cat > Main_HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCombineEval" | grep "thrpt" >> ../Main_HttpCombineEval.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpNestedFlatMapEval" | grep "thrpt" >> ../Main_HttpCombineEval.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_Main_HttpCombineEval
          path: Main_HttpCombineEval.txt

  run_jmh_benchmark_Main_HttpRouteTextPerf:
    name: Jmh Benchmark Main HttpRouteTextPerf
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@main
        with:
          path: zio-http

      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Run Benchmark
        id: run_benchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          rm -f Main_HttpRouteTextPerf.txt
          cat > Main_HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpRouteTextPerf" | grep "thrpt" >> ../Main_HttpRouteTextPerf.txt
          sbt -no-colors -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 ProbeContentTypeBenchmark" | grep "thrpt" >> ../Main_HttpRouteTextPerf.txt

      - uses: actions/upload-artifact@v3
        with:
          name: jmh_result_Main_HttpRouteTextPerf
          path: Main_HttpRouteTextPerf.txt

  jmh_publish:
    name: Jmh Publish
    needs: [run_jmh_benchmark_Current_CookieDecodeBenchmark, run_jmh_benchmark_Main_CookieDecodeBenchmark, run_jmh_benchmark_Current_HttpCombineEval, run_jmh_benchmark_Main_HttpCombineEval, run_jmh_benchmark_Current_HttpRouteTextPerf, run_jmh_benchmark_Main_HttpRouteTextPerf]
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_Current_CookieDecodeBenchmark

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_Main_CookieDecodeBenchmark

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_Current_HttpCombineEval

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_Main_HttpCombineEval

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_Current_HttpRouteTextPerf

      - uses: actions/download-artifact@v3
        with:
          name: jmh_result_Main_HttpRouteTextPerf

      - name: Create Comment
        id: create_comment
        run: |
          rm -f body1.txt
          cat > body2.txt
          rm -f body2.txt
          cat > body2.txt

      
      - name: Result Current CookieDecodeBenchmark
        id: result_current_CookieDecodeBenchmark
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body1.txt
           done < Current_CookieDecodeBenchmark.txt
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body2.txt
           done < Main_CookieDecodeBenchmark.txt 

      - name: Result Current HttpCombineEval
        id: result_current_HttpCombineEval
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body1.txt
           done < Current_HttpCombineEval.txt
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body2.txt
           done < Main_HttpCombineEval.txt 

      - name: Result Current HttpRouteTextPerf
        id: result_current_HttpRouteTextPerf
        run: |
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body1.txt
           done < Current_HttpRouteTextPerf.txt
          while IFS= read -r line; do
             IFS=' ' read -ra PARSED_RESULT <<< "$line"
             B_VALUE=$(echo "${PARSED_RESULT[1]}": "${PARSED_RESULT[4]}" ops/sec"")
             echo $B_VALUE >> body2.txt
           done < Main_HttpRouteTextPerf.txt 

      - name: Set Output
        id: set_output
        run: |
          body1=$(cat body1.txt)
           body1="${body1//'%'/'%25'}"
           body1="${body1//$'\n'/'%0A'}"
           body1="${body1//$'\r'/'%0D'}"
           echo "$body1"
           echo "::set-output name=body1::$(echo "$body1")"
           body2=$(cat body2.txt)
           body2="${body2//'%'/'%25'}"
           body2="${body2//$'\n'/'%0A'}"
           body2="${body2//$'\r'/'%0D'}"
           echo "$body2"
           echo "::set-output name=body2::$(echo "$body2")"

      
      - uses: peter-evans/commit-comment@v1
        with:
          sha: ${{github.sha}}
          body: |

            **🚀 Jmh Benchmark:**

            - **Current Branch**:
             ${{steps.set_output.outputs.body1}}

            - **Main Branch**:
             ${{steps.set_output.outputs.body2}}
             
